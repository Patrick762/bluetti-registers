The version field value is a decimal which can be decoded by combining the raw values of 4 bytes into one 32bit integer and dividing it by 100.

Example:
0x3039 is 12345 in decimal and results in 123.45 as version
